# Array Project

This project demonstrates the use of arrays in Python. The script shows how to create, access, modify, insert, and delete elements in an array.

## Prerequisites

- Python 3.x

## Installation

1. Clone the repository:
    ```sh
    git clone https://github.com/yourusername/array_project.git
    cd array_project
    ```

## Usage

Run the script to see the array operations:
```sh
python array_example.py
